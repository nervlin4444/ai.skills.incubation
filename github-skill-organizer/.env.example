---
title: Environment Variables Example
name: github-skill-organizer
description: Example .env configuration for github-skill-organizer. Defines local paths, dependency skill paths, daemon settings, and approval policies. To be copied to .env and customized per workstation.
version: "1.2.0"
github_repository: "nervlin4444/ai.skills.incubation"
target_branch: main
updated_at: "2026-05-24T22:49:00+08:00"
fixes: []
auth_config:
  provider: github
  auth_method: personal_access_token
  token_env_var: GITHUB_TOKEN
  env_file_path: ".env"
file_mapping:
  local_path: ".env.example.md"
  github_path: "github-skill-organizer/.env.example.md"
---

# ============================================
# github-skill-organizer 獨立配置
# ============================================
# 注意：GITHUB_TOKEN / GITHUB_OWNER 由依賴技能 github-restful-api-connector 提供
#       本技能自動從 DEPENDENCY_SKILL_PATH/.env 讀取

# 本地路徑（必填）
DOWNLOAD_FOLDER=~/Downloads/skills-inbox
USER_SKILLS_FOLDER=~/.workbuddy/skills

# 依賴技能路徑（必填，用於讀取其 .env 中的 GITHUB_TOKEN / GITHUB_OWNER）
DEPENDENCY_SKILL=github-restful-api-connector
DEPENDENCY_SKILL_PATH=~/.workbuddy/skills/github-restful-api-connector

# Daemon 設定（可選，有預設值）
SCAN_INTERVAL_SECONDS=60
PID_FILE_PATH=/tmp/github-skill-organizer.pid

# 審批設定（可選）
AUTO_APPROVE_PATCH=true
PATCH_MAX_FILES=3

# Web Fetcher（預設關閉，待主人確認後啟用）
ENABLE_WEB_FETCHER=false
WEB_FETCHER_INTERVAL_MINUTES=30
