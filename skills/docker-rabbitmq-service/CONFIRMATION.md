# ============================================================
# CONFIRMATION.md — 待確認項清單
# ============================================================
# 所有技能包交付前必須產出此文件，列出所有建議值供用戶確認。
# 禁止在未經用戶確認的情況下直接執行部署。
#
# 狀態標記：
#   [PENDING]      → 待用戶確認
#   [CONFIRMED]    → 用戶已確認
#   [MODIFIED]     → 用戶已修改
#   [AUTO-DETECT] → 系統自動檢測，建議用戶確認
# ============================================================

# ============================================================
# 1. 命名名稱 (Naming)
# ============================================================

## 1.1 SKILL_NAME（技能名稱）
- 建議值：docker.rabbitmq.service
- 狀態：[CONFIRMED]
- 說明：遵循規範零，使用點號分隔

## 1.2 COMPOSE_NAME（Compose 項目名）
- 建議值：rabbitmq_service
- 狀態：[AUTO-DETECT]
- 說明：從 SKILL_NAME 派生，點號改底線（Docker Compose 項目名禁止點號）

## 1.3 CONTAINER_NAME（主容器名）
- 建議值：rabbitmq.service.5672
- 狀態：[AUTO-DETECT]
- 說明：遵循規範一-B，使用點號分隔

## 1.4 VOLUME_NAME（卷名）
- 建議值：rabbitmq.data.5672
- 狀態：[AUTO-DETECT]
- 說明：遵循規範一-C，使用點號分隔

## 1.5 NETWORK_NAME（網絡名）⚠️ 動態檢測
- **檢測結果**：
  ```bash
  現有網絡：
    - nervlin.bridge.network  ← 符合 <HOST>.<TYPE>.network 格式
  ```
- **建議邏輯**：檢測到現有網絡 `nervlin.bridge.network`，建議跟隨該命名模式
- **確認值**：`nervlin.bridge.network`（跟隨現有模式）
- 狀態：[CONFIRMED] ✅
- 說明：動態檢測當前 Docker 網絡，用戶確認跟隨現有命名模式

# ============================================================
# 2. 路徑 (Paths)
# ============================================================

## 2.1 Compose 文件路徑
- 建議值：assets/docker.compose.rabbitmq.yaml
- 狀態：[CONFIRMED]

## 2.2 Docker Socket 路徑
- 檢測結果：/var/run/docker.sock
- 建議值：/var/run/docker.sock
- 狀態：[AUTO-DETECT]

## 2.3 數據卷掛載路徑
- 建議值：未使用（使用 named volume）
- 狀態：[CONFIRMED]

# ============================================================
# 3. 參數 (Parameters)
# ============================================================

## 3.1 AMQP 端口
- 建議值：5672
- 檢測結果：✅ 端口可用
- 狀態：[AUTO-DETECT]

## 3.2 Management UI 端口
- 建議值：15672
- 檢測結果：✅ 端口可用
- 狀態：[AUTO-DETECT]

## 3.3 默認用戶名
- 建議值：guest
- 狀態：[PENDING]
- 說明：生產環境必須修改

## 3.4 默認密碼
- 建議值：guest
- 狀態：[PENDING]
- ⚠️ 警告：生產環境必須修改，建議使用強密碼

# ============================================================
# 4. 確認流程
# ============================================================

## 步驟：
1. ✅ 系統自動檢測環境（容器、網絡、卷、端口）
2. ✅ 生成此 CONFIRMATION.md 文件
3. ⏳ 用戶逐項確認或修改
4. 將 [PENDING] 標記改為 [CONFIRMED] 或 [MODIFIED]
5. 系統讀取確認後的值執行部署

## 用戶確認方式：
- 回覆「確認」→ 所有 [PENDING] 項目標記為 [CONFIRMED]
- 回覆「修改 <項目> 為 <新值>」→ 該項目標記為 [MODIFIED]
- 回覆「使用 <現有網絡>」→ NETWORK_NAME 標記為 [CONFIRMED]

## 當前待確認項目：
- [1.5 NETWORK_NAME]：建議 `nervlin.bridge.network`，是否跟隨？
- [3.3 默認用戶名]：是否修改默認用戶名 `guest`？
- [3.4 默認密碼]：是否修改默認密碼 `guest`？（⚠️ 生產環境必須修改）
